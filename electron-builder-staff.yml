appId: it.oculandiavr.oculauncher.staff
productName: "OcuLauncher Staff"
copyright: © Oculandia VR
directories:
  buildResources: build
  output: dist
files:
  - src/**/*
  - package.json
asar: true
afterPack: build/afterPack.js

publish:
  provider: github
  releaseType: prerelease

win:
  icon: build/icon.png
  target:
    - target: nsis
      arch: [x64, arm64]
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  perMachine: false
  allowElevation: true
  deleteAppDataOnUninstall: false
  artifactName: OcuLauncher-Staff-Setup-${version}-win-${arch}.${ext}
  shortcutName: OcuLauncher Staff

mac:
  icon: build/icon.png
  category: public.app-category.games
  # La firma Apple resta opzionale: senza secrets in GitHub Actions la build è unsigned
  # e build/afterPack.js applica una firma ad-hoc locale per evitare il blocco "app danneggiata".
  target:
    - target: dmg
      arch: [x64, arm64]
dmg:
  artifactName: OcuLauncher-Staff-${version}-mac-${arch}.${ext}
  title: OcuLauncher Staff ${version}
